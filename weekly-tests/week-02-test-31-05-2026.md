# Week 2 Test — 31.05.2026

## Part 1 — 10 Data-Structure Questions

## Part 2 — Parser Bug-Fix Task

## Part 3 — 5-Minute Code Explanation

## Part 4 — Git Commit

## Part 5 — Self-Score

## Part 6 — Reflection

## Part 1 — 10 Data-Structure Questions

1. What is a list in Python?
List is a collection of elements which can be changed, added or deleted. List is mutable.

2. What is a tuple in Python?
Tuple is a collection of elemets which cannot be changed. Tuple is immutable.

3. What is a dictionary in Python?
Dictionary is a collection of key-value pairs

4. What is the difference between a list and a tuple?
List is mutable, tuple is immutable.

5. What does `.append()` do?
It adds the new item to the collection.

6. What does `.items()` do in a dictionary?
It repeats the code all key values in a dictionary

7. In this dictionary, how do you access the payload?
can_message["payload"]

```python
can_message = {
    "timestamp": "0.000",
    "can_id": "0x100",
    "dlc": "8",
    "payload": "11 22 33 44 55 66 77 88"
}

8. What does records[0] mean?
It returns the first item in records

9. What does records[-1] mean?
It returns the last item in records

10. What does this dictionary represent?
message_counts = {
    "0x100": 3,
    "0x200": 2,
    "0x300": 2
}
It represents the message counts of top 3 can ids in sorted from high to low.

Part 3
5-minute code explanation

In your weekly test file, write a simple explanation of your diagnostic_log_summarizer.py.

Use this structure:
## Part 3 — 5-Minute Code Explanation

My diagnostic log summarizer reads a CAN CSV file and stores each row as a dictionary inside a list called `records`.

The function `read_can_log()` opens the CSV file, reads each row using `csv.DictReader`, and stores every row in the `records` list.

The function `count_messages_by_can_id()` loops through all records, extracts the CAN ID from each record, and counts how many times each CAN ID appears.

The function `get_first_timestamp()` returns the timestamp from the first record in the log using `records[0]["timestamp"]`.

The function `get_last_timestamp()` returns the timestamp from the last record in the log using `records[-1]["timestamp"]`.

The function `get_top_active_ids()` sorts the CAN IDs based on message count and returns the most active CAN IDs.

The function `print_summary()` prints the final report, including total messages, first timestamp, last timestamp, message counts, and top active CAN IDs.

The most confusing part for me is understanding how multiple functions work together and how data moves from one function to another.

The most important thing I learned from this project is that I can now read a complete Python script, understand the flow, and explain what each function does.


## Parser Bug Fix Explanation

The bug was in the initial count value.

The broken code set a new CAN ID count to `1` and then immediately added `1` again. Because of this, every CAN ID count became one higher than expected.

I fixed the bug by setting the initial count to `0`.

Then every time the CAN ID appears, the script increases the count by `1`.

Correct logic:

```python
if can_id not in message_counts:
    message_counts[can_id] = 0

message_counts[can_id] = message_counts[can_id] + 1


---

## Part 5 — Self-Score

Lists: 6/10  
Tuples: 6/10  
Dictionaries: 6/10  
for loops: 6/10  
while loops: 6/10  
Functions: 5/10  
CSV parsing: 5/10  
Git workflow: 7/10  
Code explanation: 5/10  

## Weak Areas

1. Explaining functions clearly
2. Understanding how data moves between functions
3. Understanding `sorted()` with `lambda`

## Part 6 — Reflection

This week I learned Python data structures, functions, CSV Parsing, CAN Style diagnostic logs. Had a good understanding of the functions, the exact definitions of functions is till confusing. Need to improve the exact understanding of each function and new concept before execution.